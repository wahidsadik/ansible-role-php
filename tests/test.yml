---
- hosts: localhost
  remote_user: root
  roles:
    - ansible-role-php
- hosts: localhost
  remote_user: root
  pre_tasks:
  - local_action: command ansible-galaxy install wahidsadik.ansible-role-apache
  roles:
    - { role: wahidsadik.ansible-role-apache }
    - { role: ansible-role-php, integrate_with_apache: true }
# - hosts: localhost
#   remote_user: root
  # pre_tasks:
  # - local_action: command ansible-galaxy install wahidsadik.TBD
#   roles:
#     - apache-role-apache
#     - { role: username.ansible-role-php, integrate_with_mysql: true }
# - hosts: localhost
#   remote_user: root
  # pre_tasks:
  # - local_action: command ansible-galaxy install wahidsadik.apache-role-apache
  # - local_action: command ansible-galaxy install wahidsadik.tbd
#   roles:
#     - apache-role-apache
#     - { role: username.ansible-role-php, integrate_with_apache: true, integrate_with_mysql: true }
