---
- hosts: localhost
  remote_user: root
  roles:
    - ansible-role-php
- hosts: localhost
  remote_user: root
  roles:
    - wahidsadik.apache-role-apache
    - { role: ansible-role-php, integrate_with_apache: true }
# - hosts: localhost
#   remote_user: root
#   roles:
#     - apache-role-apache
#     - { role: username.ansible-role-php, integrate_with_mysql: true }
# - hosts: localhost
#   remote_user: root
#   roles:
#     - apache-role-apache
#     - { role: username.ansible-role-php, integrate_with_apache: true, integrate_with_mysql: true }
